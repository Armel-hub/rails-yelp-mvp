<h1>All our restaurants</h1>

<p><%= link_to 'Add new restaurant', new_restaurant_path, class: 'btn btn-primary' %></p>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
    </li>
  <% end %>
</ul>


